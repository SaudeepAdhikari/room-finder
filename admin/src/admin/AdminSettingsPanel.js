import React from 'react';

function AdminSettingsPanel() {
    return (
        <div style={{ padding: 40 }}>
            <h1>Settings removed</h1>
            <p>The global admin settings feature has been removed. Configuration is now handled directly in code or via environment variables.</p>
        </div>
    );
}

export default AdminSettingsPanel;